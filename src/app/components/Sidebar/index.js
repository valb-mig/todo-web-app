import SidebarRoot  from './config/SidebarRoot';
import SidebarStart from './config/SidebarStart';
import SidebarEnd   from './config/SidebarEnd';
import SidebarBox   from './config/SidebarBox';

const Sidebar = {

    Root:  SidebarRoot,
    Start: SidebarStart,
    End:   SidebarEnd,
    Box:   SidebarBox
}

export default Sidebar;