import './styles/SidebarEnd.scss';

const SidebarEnd = ({ children }) => {
    return(
        <section className={"sidebar-end"}>
            { children }
        </section>
    );
}

export default SidebarEnd;