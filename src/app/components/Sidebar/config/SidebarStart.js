import './styles/SidebarStart.scss';

const SidebarStart = ({ children }) => {
    return(
        <section className={"sidebar-start"}>
            { children }
        </section>
    );
}

export default SidebarStart;