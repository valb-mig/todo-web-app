import HeaderRoot   from './config/HeaderRoot';
import HeaderStart  from './config/HeaderStart';
import HeaderEnd    from './config/HeaderEnd';
import HeaderSearch from './config/HeaderSearch';

const Header = {

    Root:   HeaderRoot,
    Start:  HeaderStart,
    End:    HeaderEnd,
    Search: HeaderSearch
}

export default Header;