import ModalRoot   from './config/ModalRoot';
import ModalHeader from './config/ModalHeader';
import ModalBody   from './config/ModalBody';
import ModalFooter from './config/ModalFooter';

const Modal = {
    Root:   ModalRoot,
    Header: ModalHeader,
    Body:   ModalBody,
    Footer: ModalFooter
}

export default Modal;