import TaskRoot   from './config/TaskRoot';
import TaskTitle  from './config/TaskTitle';
import TaskDesc   from './config/TaskDesc';
import TaskOption from './config/TaskOption';
import TaskInfo   from './config/TaskInfo';

const Task = {

    Root:   TaskRoot,
    Title:  TaskTitle,
    Desc:   TaskDesc,
    Option: TaskOption,
    Info:   TaskInfo
}

export default Task;